# THE FADE — New-Chat Super Bundle (one-file handoff)

PROMPT: #418 - fade-phase3-build-single-new-chat-handoff-super-bundle  
LABEL: THE_FADE_PHASE3_BUILD_SINGLE_NEW_CHAT_HANDOFF_SUPER_BUNDLE  
TRANCHE: #117 - Build Single New-Chat Handoff Super Bundle  

---

## 1. Title, timestamp, branch

- **Title:** THE FADE — New-Chat Super Bundle (one-file handoff).
- **Updated:** 2026-04-09T22:30:00+00:00  
- **Branch (verify):** `the-fade-phase1-tranche1-foundation` — `git branch --show-current`.

**Scope:** Single file for pasting into a **new** chat. Does not replace `THE_FADE_CONTEXT_ANCHOR.md`, `THE_FADE_HANDOFF_BUNDLE_LATEST.md`, or other canon.

---

## 2. Plain-English current checkpoint

- **T116** build is **in the repo** on disk: `build_source_records_snapshot_from_run_folder.py` + README; `source_records_snapshot.json` per run when executed (**Prompt #415**).
- **T117** governance is **on disk** (**Prompt #417**): T117 MD pair, `phase3_t117_first_real_scanner_rule.json`, `post_t117_*` in `phase2_mvp_approval_scope_decision.json`, updated control docs. **No** rule evaluation script yet.
- **T117 is NOT git-locked:** **#417** (and follow-on doc work) is **not** treated here as committed/pushed; verify with `git status` / `git log`.
- **This file** is the deliverable of **Prompt #418** (`BUILD_SINGLE_NEW_CHAT_HANDOFF_SUPER_BUNDLE`) — **documentation only**, no git in that prompt.
- **Next chat — first execution priority:** run the **git-only** lock for the T117 slice **before** any T118 or T117 **implementation** work.
- **Do not start T118** (or implement `build_first_real_scanner_rule_from_run_folder.py`) until T117 governance is **git-locked** per operator list.

---

## 3. Phase ladder

- [x] Phase 2 — scoped MVP approval (**T80**); lane posture per anchor (verify `mvp_lane_approval.json`).
- [x] Phase 3 — contracts through local happy-path runner; operator review/attention queue; **T115** governance; **T116** capture **build**.
- [x] **T117** — first real scanner rule **governance** (docs + JSON + scope note) — **on disk**.
- [x] **YOU ARE HERE** — T117 governance **exists in workspace**; **git lock outstanding**; **#418** handoff bundle **authored** (this file).
- [ ] **Git-only lock** — narrow stage/commit/push for T117 (+ this bundle if included). See §6.
- [ ] **Future** — One tranche may add `build_first_real_scanner_rule_from_run_folder.py` + `first_real_scanner_rule_evaluation.json` (T117 acceptance); **not** T118 unless re-governed.
- [ ] **Not done** — Scanner **engine**, ranking/selection, runtime/dashboard **platforms**, lane reopen, registry edits without governance.

---

## 4. Locked operating rules

- Prompt numbers advance **sequentially**; every THE FADE Cursor prompt carries **PROMPT / LABEL / TRANCHE**; repeat at **top and bottom** of replies when required.
- Show the **phase ladder** when continuing; **score reviewed replies** when discipline is on.
- **First principles / least steps** — smallest honest move.
- **Value #1.5:** “Short steps to a pile of shit makes for a messy shoe. And you smell like shit.”
- **Tradesman cleanup:** leave the bench clean — clear unrelated **git** staging before a lock prompt.
- **Order:** governance → build → **git lock** when authorized. **Do not widen** tranche scope.
- **Do not touch** `mvp_lane_approval.json` or `mvp_lane_evidence_registry.json` unless explicitly governed.
- **Timestamps:** update **Updated** on edited docs; **no future-dated** timestamps.
- **Git:** isolate staging — only operator-approved paths for a lock prompt.

---

## 5. Exact current truth for the next chat

| Topic | Truth |
|--------|--------|
| **Built / in repo (typical)** | Scripts and artifacts through **T116** per `phase2_mvp_approval_scope_decision.json` and master checklist. |
| **On disk, not assumed committed** | T117 governance files, control-doc updates from **#417**, this super bundle after **#418**. |
| **Not built** | `build_first_real_scanner_rule_from_run_folder.py`; scanner engine; ranking stack. |
| **Why next is not #419 for “first lock”** | Original gate plan called the **first** post-#417 execution **#418** = **git-only** T117 lock. **This** Cursor prompt **#418** instead delivered **this** handoff file (no git). **Mechanical next step** = same **git-only** lock; **prompt number** = **#418** in the original plan **or** **#419** if your ledger already consumed **#418** for this bundle (see §6). |

---

## 6. NEXT ACTION IN THE NEW CHAT

- **Prompt #418** (original plan) — **Purpose:** lock **T117 governance only** — **git-only** (stage / commit / push narrow file list). **No** content edits beyond typos if needed. **No T118** until this completes.
- **Numbering collision (honest):** If **#418** is already used for **this** super-bundle authoring session, run the **identical git-only lock** as **Prompt #419** — **never** two different meanings for one number in the same ledger.
- **No** implementation work in the same prompt as the lock unless operator explicitly combines (default: **separate**).

---

## 7. Exact relevant file map

| Path | Why it matters |
|------|----------------|
| `future_modules/the_fade/docs/THE_FADE_CONTEXT_ANCHOR.md` | One-screen checkpoint. |
| `future_modules/the_fade/docs/THE_FADE_PROCESS_CHECKLIST.md` | Process posture. |
| `future_modules/the_fade/docs/THE_FADE_HANDOFF_BUNDLE_LATEST.md` | Narrative handoff. |
| `future_modules/the_fade/docs/THE_FADE_PHASE2_REMAINING_GATE_PLAN.md` | Locked sequence / partialities. |
| `future_modules/stock_module/JARVIS_THE_FADE_MASTER_BUILD_CHECKLIST.md` | Master tranche table. |
| `future_modules/the_fade/docs/THE_FADE_T117_FIRST_REAL_SCANNER_RULE_GOVERNANCE.md` | T117 rule boundary. |
| `future_modules/the_fade/docs/THE_FADE_T117_FIRST_REAL_SCANNER_RULE_ACCEPTANCE_CRITERIA.md` | Future implementation PASS/FAIL. |
| `future_modules/the_fade/config/phase3_t117_first_real_scanner_rule.json` | Pinned paths / rule id / no network. |
| `future_modules/the_fade/config/phase2_mvp_approval_scope_decision.json` | `post_t117_*` governance note. |
| `future_modules/the_fade/review/local_run/build_source_records_snapshot_from_run_folder.py` | T116 — produces `source_records_snapshot.json`. |
| `future_modules/the_fade/review/local_run/README_source_records_snapshot.md` | T116 usage. |
| `future_modules/the_fade/outputs/local_happy_path_runs/run_*/source_records_snapshot.json` | Input for **future** T117 rule script (if present). |
| `future_modules/the_fade/docs/THE_FADE_NEW_CHAT_SUPER_BUNDLE.md` | **This** one-file handoff. |

---

## 8. Branch / workspace hygiene warning

- Workspace may have **unrelated** dirty or untracked files.  
- **Clear** unrelated **staged** changes before a lock prompt; stage **only** approved paths.  
- Never `git add .` for a narrow governance lock.

---

## 9. Next-number continuity

- **Last THE FADE governance authoring prompt on disk:** **#417** (T117 governance package + control updates).  
- **This file:** authored under **#418** — `THE_FADE_PHASE3_BUILD_SINGLE_NEW_CHAT_HANDOFF_SUPER_BUNDLE`.  
- **Next execution:** **#418** (git-only T117 lock) **per original gate plan**, **or** **#419** if **#418** is reserved for this bundle in your prompt log — **do not** reuse older numbers or drift backward.

---

## 10. One-line handoff summary

**T116 is built; T117 governance is on the filesystem but not git-locked — run a narrow git-only lock (#418 per plan, or #419 if #418 is this bundle) before any T117 implementation or T118, using this file plus the anchor and T117 docs as truth.**

---

PROMPT: #418 - fade-phase3-build-single-new-chat-handoff-super-bundle  
LABEL: THE_FADE_PHASE3_BUILD_SINGLE_NEW_CHAT_HANDOFF_SUPER_BUNDLE  
TRANCHE: #117 - Build Single New-Chat Handoff Super Bundle  
