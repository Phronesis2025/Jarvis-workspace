{
  "_meta": {
    "title": "mvp_lane_evidence_registry",
    "prompt_number": 321,
    "phase_number": 2,
    "tranche_number": 71
  },
  "module": "the_fade",
  "phase": 2,
  "tranche": 71,
  "approval_state": {
    "approval_authority": "future_modules/the_fade/config/mvp_lane_approval.json",
    "approved": false,
    "approved_mvp_lanes": [],
    "deferred_lanes": [
      "lane_a_public_signal",
      "lane_b_official_disclosure",
      "lane_c_market_context",
      "lane_e_research_swarm_context"
    ],
    "decision_mode": "evidence_collection_only"
  },
  "evidence_dimensions": {
    "reliability": {
      "what_to_evidence": "Adapter/source outage rates are low enough that the lane is not dominated by failures during the pre-audit window."
    },
    "freshness": {
      "what_to_evidence": "Evidence items carry timestamps that can be evaluated as fresh vs stale using a documented freshness window."
    },
    "normalization_viability": {
      "what_to_evidence": "Raw lane events can be normalized into `normalized_signal_event` without silent drops."
    },
    "stale_outage_behavior": {
      "what_to_evidence": "When the lane is stale/unavailable, the system behavior is explicit (downgrade confidence or escalate per policy) without fabricating values."
    },
    "conflict_handling": {
      "what_to_evidence": "Conflicting lane evidence is handled without corrupting direction models or forcing context-only data to override primary truth."
    },
    "context_dominance_risk": {
      "what_to_evidence": "Research Swarm context-only reads do not dominate or override primary lane truth."
    }
  },
  "lanes": [
    {
      "lane_id": "lane_a_public_signal",
      "status": "deferred_pending_evidence",
      "required_evidence": [
        "reliability",
        "freshness",
        "normalization_viability",
        "stale_outage_behavior",
        "conflict_handling",
        "context_dominance_risk"
      ],
      "evidence_status": "partial",
      "dimension_evidence_status": {
        "reliability": "partial",
        "freshness": "not_started",
        "normalization_viability": "partial",
        "stale_outage_behavior": "not_started",
        "conflict_handling": "not_started",
        "context_dominance_risk": "not_started"
      },
      "last_evidence_recorded_at": "2026-04-05T01:32:07+00:00",
      "notes": "Tranche 64 (Prompt #294): bounded charter `docs/LANE_A_PHASE2_EVIDENCE_CHARTER_T64.md` (decision #293). Tranche 65 (Prompt #298): **one** live bounded **`lane_b_real_observation_slice.py observe`** pass with `--source-lane lane_a_public_signal` to locked HTTPS JSON `https://api.coinbase.com/v2/exchange-rates?currency=BTC` (no auth; operator ToS/rate limits operator-owned). Outcome **success-only** on this run: `t65_lane_a_first_observe_normalized_signal_event.json` under `outputs/lane_a_public_signal/` (directory `*.json` gitignored — committed record in `MVP_LANE_EVIDENCE_LOG.md`). **Does not** prove observe-path failure modes on this run, other dimensions, multi-attempt reliability, stale/outage, conflict, or context dominance; **does not** close Lane A or grant MVP approval. Tranche 66 (Prompt #302): **Lane A slice-1 STOP** — no further Lane A evidence tranches unless a **new explicit Lane A charter** (governed prompt); Lane A remains **`partial`**, not MVP-closed. `mvp_lane_approval.json` unchanged."
    },
    {
      "lane_id": "lane_b_official_disclosure",
      "status": "evidence_in_progress_partial",
      "required_evidence": [
        "reliability",
        "freshness",
        "normalization_viability",
        "stale_outage_behavior",
        "conflict_handling",
        "context_dominance_risk"
      ],
      "evidence_status": "partial",
      "dimension_evidence_status": {
        "reliability": "partial",
        "freshness": "partial",
        "normalization_viability": "partial",
        "stale_outage_behavior": "partial",
        "conflict_handling": "partial",
        "context_dominance_risk": "partial"
      },
      "last_evidence_recorded_at": "2026-04-04T13:26:53+00:00",
      "notes": "Lane B remains the deepest evidenced lane but still partial and unapproved. Tranche 30 captured the full FR window on disk (22/22/0, with `t30_valid_002` excluded). Tranches 31-33 locked freshness policy discipline (`strict_midnight_utc`) with explicit 10/22 cannot-classify limits. Tranche 34 (normalization breadth) and Tranche 35 (stale/outage + escalation alignment) are executed but partial for gate closure. Tranche 36 cross-lane rollup confirms Lane B is strongest but still not gate-complete. Tranche 45 (`audit_lane_b_failure_path_stale_outage_trace.py`) — LOCAL fixtures: failure-path / stale-outage policy trace; does not close standard #4 at production scale. Tranche 47 (`audit_lane_b_conflict_fusion_precedence_trace.py`) — LOCAL fixtures: Lane B vs context-only fusion precedence trace aligned to `lane_b_real_observation_slice.py` conflict semantics + `fusion_policy.json`; explicit non-override wording for valid context; missing/wrong-role blocked explicitly; stale context age not consumed by minimal conflict slice (documented gap). Tranche 50 (`audit_lane_b_normalization_viability_silent_drop_trace.py`) — LOCAL fixtures: representative normalization success, normalization-blocked, missing-required-field omission, and invalid-candidate scout_failure paths are all explicit with no silent drop observed in the bounded cases. This reduces the exact `normalization_viability` silent-drop gap but does not prove full live normalization breadth, full runtime coverage, or MVP approval. Tranche 52 (`audit_lane_b_stale_context_conflict_omission_trace.py`) — ADOPTED PRE-EXISTING LOCAL fixtures: bounded stale-first omission wrapper for Lane B conflict-style handling. In the bounded stale-context cases, omission is explicit, stale context does not influence or override Lane B primary truth, and fresh valid context remains a separate conflict-handling branch. This does not prove that the current minimal `lane_b_real_observation_slice.py conflict` subcommand itself consumes freshness fields, and it does not close full conflict/runtime coverage or MVP approval. Tranche 54 (`audit_lane_b_stale_outage_residual_policy_coverage_trace.py`) — LOCAL fixtures plus prior T35/T45 outputs: bounded explicit coverage for the exact residual escalation-policy classes `UNDEFINED_DIRECTION_MODEL` and `MISSING_REQUIRED_LANE` that were still not evidenced in the stored FR slice after T35 and not already covered by T45. This reduces the residual standard #4 policy-row coverage gap and shows non-silent handling for those classes, but it does not prove live FR outage evidence, production-scale stale/outage closure, whole-gate closure, or MVP approval. Tranche 56 (`audit_lane_b_minimal_conflict_freshness_consumption_truth.py`) — LOCAL code-path inspection plus bounded replay of the real `lane_b_real_observation_slice.py conflict` subcommand. The minimal path reads `source_lane`, `semantic_role`/`role`, and `direction_hint`, but not freshness-like fields; fresh-vs-stale replay pairs with identical directional inputs emit the same conflict-packet content, so valid stale-labeled context is still consumed by the minimal path rather than omitted there. This narrows the exact conflict/freshness truth gap by proving T52 stale-first omission remains wrapper-only relative to the current minimal path. Tranche 58 (`audit_lane_b_real_slice_normalization_truth.py`) — STORED 22-SLOT FR SLICE ONLY: exact collector retention plus partial normalization support are now evidenced on the real slice (`task_id`, timing, evidence URL/path, `response_sha256`, and `results[0]` identity fields), and collector-level silent disappearance is not observed on the stored 22-slot population. This does not prove full `normalized_signal_event` materialization without policy fill-ins or invented values, does not prove full raw-body preservation beyond the truncated preview boundary, does not close full live normalization breadth/runtime coverage, and does not grant MVP approval. Tranche 60 (`audit_lane_b_real_slice_conflict_fusion_truth.py`) — STORED 22-SLOT FR SLICE ONLY plus THE FADE-local `inputs/lane_b_real_evidence/context_only_contra.example.json` (no new fixture; no network): proves collector JSONL/snapshots carry task/timing/endpoint/`response_sha256`/preview material but do not store observe-output `{task_id}_normalized_signal_event.json` files, do not store `source_lane` or `direction_hint` as snapshot top-level fields, and therefore do not by themselves determine direction mismatch vs contra unless `direction_hint` is policy-filled (e.g. observe default `neutral`). If `direction_hint` is omitted on a synthesized lane artifact, `cmd_conflict` reports `mismatch=False` even against bearish local contra; this is a mechanical honesty boundary, not a claim that FR content is non-directional. Minimal `cmd_conflict` still emits explicit primary=lane_b + `fusion_policy.json` weight wording under bounded replay; does not prove production fusion runtime, live Lane E integration, or gate closure for conflict_handling / context_dominance_risk. Tranche 61 (Prompt #276) — GOVERNANCE POLICY ONLY: `lane_b_phase2_conflict_replay_policy_decision.json` locks Phase 2 operator-facing classification of slice-derived bytes vs policy_fill vs unknown when replaying `lane_b_real_observation_slice.py conflict` on stored 22-slot collector artifacts; requires explicit labeling when mismatch conclusions depend on policy-filled `direction_hint`; does not close conflict_handling or context_dominance_risk; does not grant MVP approval. Tranche 62 (Prompt #283) — CONTROLLED Protocol A: `lane_b_controlled_evidence_harness.py` + T62 inputs under `examples/lane_b_controlled_evidence_harness_inputs/t62_protocol_a_*.json`; one `LANE_B_STALE_UNAVAILABLE_CONTROLLED_REPLAY_V1` observation with harness `observed_at` UTC + `evidence_item_timestamp`; `observed_behavior` escalate per operator scenario; **no** vendor HTTP; raw JSON mirror path `outputs/lane_b_real_observation/t62_protocol_a_lane_b_controlled_observation.json` (outputs `*.json` gitignored — see log embed); **not** production stale/outage or standard #4 closure; does not grant MVP approval. Tranche 63 (Prompt #288) — REAL observe path: `lane_b_real_observation_slice.py observe` to Federal Register documents.json (HTTPS, one attempt); outcome success-only (`normalized_signal_event`, HTTP 200); local artifact `t63_real_observe_20260404T130000Z_normalized_signal_event.json` under `outputs/lane_b_real_observation/` (gitignored `*.json` — committed record in MVP_LANE_EVIDENCE_LOG.md); does not evidence scout_failure, timeout, or empty-row on this run; does not close stale_outage_behavior / standard #4; does not grant MVP approval. Post-T64 (Prompt #294): **Lane B default freeze** — new Lane B evidence tranches **paused by default**; reopen **only** under a **new governed Lane B charter** naming scope and bounds. Tranche 66 (Prompt #302): freeze language **unchanged**; **no** Lane B reopen."
    },
    {
      "lane_id": "lane_c_market_context",
      "status": "deferred_pending_evidence",
      "required_evidence": [
        "reliability",
        "freshness",
        "normalization_viability",
        "stale_outage_behavior",
        "conflict_handling",
        "context_dominance_risk"
      ],
      "evidence_status": "partial",
      "dimension_evidence_status": {
        "reliability": "partial",
        "freshness": "not_started",
        "normalization_viability": "partial",
        "stale_outage_behavior": "not_started",
        "conflict_handling": "not_started",
        "context_dominance_risk": "not_started"
      },
      "last_evidence_recorded_at": "2026-04-06T14:22:09+00:00",
      "notes": "Tranche 66 (Prompt #302): bounded **live** evidence charter on disk — `docs/LANE_C_PHASE2_EVIDENCE_CHARTER_T66.md` — **governance/doc only**; **no** Lane C observation in T66. **Historical (on disk, local fixtures only):** Tranche 41 + Tranche 42; Tranche 43 PATH B pause — **not** live market integration. Tranche 67 (Prompt #307): **one** bounded **`lane_b_real_observation_slice.py observe`** pass with **`--source-lane lane_c_market_context`** to locked HTTPS JSON **`https://api.frankfurter.app/latest?from=USD`** (Frankfurter public ECB-based rates; **GET**, no auth; operator ToS/rate limits operator-owned). Outcome **success-only** on this run: `t67_lane_c_first_observe_normalized_signal_event.json` under `outputs/lane_c_market_context/` (directory `*.json` gitignored — committed record in `MVP_LANE_EVIDENCE_LOG.md`). **First-touch dimensions on this run:** **reliability** + **normalization_viability** → **`partial`** (single attempt; explicit `normalized_signal_event` vs explicit failure paths exist in tool but **not** exercised here). **Does not** prove multi-attempt reliability, observe-path failure modes on this run, freshness discipline, stale/outage, conflict, or context dominance; **does not** close Lane C or grant MVP approval. Tranche 68 (Prompt #311): **Lane C slice-1 STOP** — no further Lane C live evidence tranches unless a **new explicit Lane C charter** (governed prompt); **`partiality` is preserved, not closed** — Lane C remains **`evidence_status` `partial`**, **not** MVP-closed; **does not** imply approval, gate closure, or Phase 3 readiness. `mvp_lane_approval.json` unchanged."
    },
    {
      "lane_id": "lane_e_research_swarm_context",
      "status": "deferred_pending_evidence",
      "required_evidence": [
        "reliability",
        "freshness",
        "normalization_viability",
        "stale_outage_behavior",
        "conflict_handling",
        "context_dominance_risk"
      ],
      "evidence_status": "partial",
      "dimension_evidence_status": {
        "reliability": "partial",
        "freshness": "not_started",
        "normalization_viability": "partial",
        "stale_outage_behavior": "not_started",
        "conflict_handling": "not_started",
        "context_dominance_risk": "not_started"
      },
      "last_evidence_recorded_at": "2026-04-06T15:31:07+00:00",
      "notes": "**Historical (on disk, THE FADE-local fixtures only — not live HTTPS):** Tranches **37–39** bounded audits (`audit_lane_e_*` + `examples/lane_e_context_bootstrap/`) — **not** live Research Swarm integration. Tranche **39A** PATH B: Lane E bootstrap **paused** after bounded value capture. **T69** (Prompt **#314**): bounded **live** evidence charter — `docs/LANE_E_PHASE2_EVIDENCE_CHARTER_T69.md`. **T70** (Prompt **#317**): **one** bounded **`lane_b_real_observation_slice.py observe`** pass with **`--source-lane lane_e_research_swarm_context`** to locked HTTPS URL **`https://api.crossref.org/works/10.1038/d41586-019-02658-z`** (Crossref public Works API; **GET**, no auth in repo; operator ToS/rate limits operator-owned). **Observed outcome class:** **`scout_failure`** — HTTP **404**, `error_type` **`SOURCE_UNAVAILABLE`**; local artifact `t70_lane_e_first_observe_scout_failure.json` under `outputs/lane_e_research_swarm_context/` (`*.json` gitignored — committed record in `MVP_LANE_EVIDENCE_LOG.md`). **First-touch dimensions on this run:** **reliability** + **normalization_viability** → **`partial`** (single attempt; explicit failure path materialized — **not** success-only). **T71** (Prompt **#321**): **Lane E slice-1 STOP** — **governance/doc lock only**; **no** new live observation; **no** collection. **No** further Lane E **live** evidence tranches unless a **new explicit Lane E charter** (governed prompt). **`partiality` is preserved, not closed** — Lane E remains **`evidence_status` `partial`**, **not** MVP-closed at gate bar; **does not** imply approval, gate closure, or Phase 3 readiness. **Does not** prove HTTP **200** / `normalized_signal_event` for the T70 URL on any run beyond what T70 already recorded. `mvp_lane_approval.json` unchanged."
    }
  ],
  "evidence_collection_rules": {
    "scope": "Pre-approval evidence only. This registry does not grant MVP approval and must not be interpreted as an approval statement.",
    "collection_window": "Use the same pre-audit window assumed by `MVP_SOURCE_RELIABILITY_AUDIT.md` and record observations with timestamps and failure-mode details.",
    "dimension_requirements": {
      "reliability": "Record failure counts/outage observations sufficient to evaluate against `required_reliability_threshold` (0.8 in `mvp_lane_approval.json`).",
      "freshness": "Record observed evidence timestamps and classify fresh vs stale according to the gate freshness window (define the window in the log).",
      "normalization_viability": "Record normalization outcomes: success vs silent-drop indicators, tied to sample inputs.",
      "stale_outage_behavior": "Record explicit behavior on stale/unavailable conditions (downgrade/escalate/omit) with an example per lane.",
      "conflict_handling": "Record how conflicting evidence is resolved without letting context-only enrichment override primary truth.",
      "context_dominance_risk": "Record checks that context-only reads do not dominate primary lane truth."
    }
  },
  "notes": "Reconciled through Tranche 71 (Prompt #321): **T71** — Lane E **slice-1 STOP** governance lock on disk (`LANE_E_PHASE2_EVIDENCE_CHARTER_T69.md` §12); **no** new collection; Lane E **`evidence_status` remains `partial`** (dimensions unchanged); **`mvp_lane_approval.json` unchanged** (`approved:false`). Prior: T70 Lane E live observe, etc. **No** MVP lane is **active-by-default** for the next bounded slice; next Phase **2** spend requires **operator-governed** next-lane or approval-scope decision. Keep `approved:false` until evidence demonstrates the full gate standard."
}

